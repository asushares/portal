(function(window) {
    // Environment variables
    window["CONSENT_BUILDER_DEFAULT_FHIR_URL"] = "${CONSENT_BUILDER_DEFAULT_FHIR_URL}";
    window["CONSENT_CDS_ROOT_URL"] = "${CONSENT_CDS_ROOT_URL}";
})(this);